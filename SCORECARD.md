# UI9000 engine scorecard

<!-- Generated by scripts/scorecard.ts. Do not edit by hand: run `yarn scorecard`. -->

Every number below is read back from one `vitest run` over `tests/eval/dimensions`
and `tests/adversarial`. Nothing here is estimated or typed in.

**Status: PASS**

## Eval cases by dimension

Floor: 50 cases in total, 8 in every dimension.

| Dimension | Cases | Pass | Fail | Floor |
| --- | ---: | ---: | ---: | --- |
| selection | 19 | 19 | 0 | ok (>= 8) |
| schema | 12 | 12 | 0 | ok (>= 8) |
| data_accuracy | 12 | 12 | 0 | ok (>= 8) |
| a11y | 12 | 12 | 0 | ok (>= 8) |
| policy | 21 | 21 | 0 | ok (>= 8) |
| **total** | **76** | **76** | **0** | ok (>= 50) |

Suite guards on the eval set itself (closed dimension enum, closed intents, every
policy probe exercised, fixture parsing): 17/17 pass.

## Adversarial suite

**9/9** — one hostile spec per validation code,
each refused with the exact code before any render path is touched.

| Case | Validation code | Result |
| --- | --- | --- |
| 01-unknown-component | `unknown_component` | refused |
| 02-javascript-url | `javascript_url` | refused |
| 03-data-url | `data_url` | refused |
| 04-handler-prop | `handler_prop` | refused |
| 05-missing-bound-field | `missing_bound_field` | refused |
| 06-unmet-data | `unmet_data` | refused |
| 07-unlabelled-control | `unlabelled_control` | refused |
| 08-unknown-action | `unknown_action` | refused |
| 09-unnamed-tool | `unnamed_tool` | refused |

Suite guards on the adversarial set itself (one case per code, a fixture per case,
no skips, no render path): 5/5 pass.

## Not measured here

- **Governance (S4-11).** No governance numbers are reported, because no governance
  suite exists in `tests/` yet — the generator checks, rather than assuming. They are
  absent rather than estimated. When S4-11 lands, this scorecard fails until it grows
  a row for it, so the claim cannot go stale quietly.

